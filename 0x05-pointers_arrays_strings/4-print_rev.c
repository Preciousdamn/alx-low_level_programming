#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * print_rev - prints a string in reverse order
 * @s: pointer of parameter
 * Return: Nothing
 */

void print_rev(char *s)
{
	int length = strlen(s);

	while (len--)
		_putchar(*(s + len));
	_putchar(10);
}
